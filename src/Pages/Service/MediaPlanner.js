import React from 'react';
import '../../App.css';
import AppLayouts from '../../Layouts/AppLayouts';

function MediaPlanner() {
    return (
        <AppLayouts title="Media Planner">
            <div className="text-3xl font-bold text-gray-900 dark:text-white">Media Planner</div>
        </AppLayouts>
    );
}

export default MediaPlanner;
