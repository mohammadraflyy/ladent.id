import React from 'react';
import '../App.css';
import AppLayouts from '../Layouts/AppLayouts';

function Contact() {
    return (
        <AppLayouts title="Contact">
            <div className="text-3xl font-bold text-gray-900 dark:text-white">Contact</div>
        </AppLayouts>
    );
}

export default Contact;
